/// @file commands.h
/// @brief Phase 1 IPC command registration.
#pragma once

namespace commands {

/// @brief Register all Phase 1 commands (ping, simulate, get_binary).
void register_all();

} // namespace commands
